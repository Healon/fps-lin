# PROJECT 96：程序化微型網頁 FPS 計畫書暨規格表

> 版本：v1.0（2026-08-04）
> 狀態：已對焦。四項核心決策已由 Lin 裁決（見 §2）。
> 本檔整併原 GPT 規格的十六份文件為單一計畫書，日後團隊或範圍擴大時再依 §12 拆分。
> 工作代號 PROJECT 96 致敬 96KB 精神，正式名稱於發行前另定，不使用 .kkrieger（見 §11）。

---

## 0 一頁摘要

做一款**純瀏覽器單人 FPS**：玩家開網址即玩，無帳號、無後端、無下載。所有材質、模型、關卡、音效、音樂都在載入時由**固定種子程序化生成**，repo 內**零素材檔**（無任何 png、wav、glb，以 CI 強制）。風格為黑暗科幻加生物機械，單一線性關卡，首次通關 15 至 30 分鐘。

兩條靈魂鐵則，貫穿全案：

1. **資產零素材檔**：所有可見可聽的內容都來自程式與參數，CI 檢查守門（§7.5）。
2. **影響玩法的生成一律 CPU 決定性**：關卡佈局、碰撞、敵人配置在 CPU 上以固定種子生成，跨機器逐位元一致；只影響外觀的生成才允許上 GPU，接受廠商間的微小視覺差異（§6.4）。

路線圖：M0 技術骨架 → M1 概念驗證 → M2 垂直切片 → M3 完整內容 → M4 容量效能優化（≤1MB）→ M5 發行候選 → M6 壓縮挑戰版（256KB 與 96KB，選配）。

---

## 1 產品定位

### 1.1 願景

以 demoscene 的技術精神做一款「開連結就能玩完」的完整 FPS。它同時是遊戲與技術展示：玩家看到的是一款黑暗科幻短篇射擊遊戲，開發者看到的是「這整個世界的傳輸量比一張手機照片還小」。

### 1.2 核心價值

1. 極小傳輸容量（以 brotli 壓縮後的實際傳輸量計，§7.1）。
2. URL 即玩：點連結、按一下開始、進入遊戲，全程無帳號無後端。
3. 程序化生成材質、模型、特效、音效與音樂。
4. 固定種子、固定關卡、固定流程：每個玩家的體驗一致。
5. 黑暗、工業、哥德與科幻融合的視覺風格。
6. 技術展示與遊戲體驗並重：每個里程碑都以「好不好玩」驗收，不只以「技術能不能跑」驗收。

### 1.3 目標玩家

復古 FPS 玩家、demoscene 與程式生成愛好者、遊戲開發者與電腦圖學愛好者、喜歡短篇實驗性遊戲的玩家、js13kGames 與相關社群。

### 1.4 非目標（第一版明確不做）

多人連線。開放世界。隨機生成關卡。劇情分支。角色養成。內購與商城。帳號系統。語音演出。存檔系統（死亡採重生機制，§3.2）。**行動裝置與觸控操作**（偵測到觸控裝置時顯示「本遊戲需要鍵盤與滑鼠」的說明頁）。生成式 AI 即時產生內容。

### 1.5 成功標準（發行版驗收，M5 逐條核對）

1. 玩家能從開始畫面進入並完成完整關卡。
2. 至少三種不同戰鬥情境（走廊遭遇、大廳混戰、首領戰）。
3. 至少三把武器、三種敵人加一名首領。
4. 所有材質由程序生成、所有模型由程序生成、主要音效與音樂由合成器生成。
5. repo 內零素材檔（CI 綠）。
6. 傳輸容量符合當前階段目標（M5 時 ≤1MB）。
7. 測試矩陣內的瀏覽器與硬體無阻斷性錯誤。
8. 外部測試玩家能在沒有開發者解說下完成遊戲。
9. 玩家能清楚辨識武器、敵人、出口與可互動物件。
10. 載入生成時間在目標內（§7.4）。
11. 具備獨立美術風格，而非直接仿製參考作品。

---

## 2 核心設計決策

### 2.1 已裁決（Lin，2026-08-04）

| 編號 | 決策 | 裁決 | 理由 |
|---|---|---|---|
| D-001 | 部署型態 | **網頁版，開網址即玩**。部署至 Netlify，桌面瀏覽器執行 | 「可部署」的本意。macOS 開發零障礙，沿用既有 Netlify 部署流程。原生 exe 挑戰版列為遠期選配（M6 之後另議） |
| D-002 | 容量目標 | **階梯式：先玩得動再壓**。MVP 不設嚴限，完整版 ≤1MB，之後逐階挑戰 256KB 與 96KB | 避免「省了 30KB 但遊戲沒敵人」。GPT 風險表自認「追求容量導致不好玩」是高影響高機率 |
| D-003 | 技術路線 | **手寫 WebGL2 自製微引擎**，不用 Three.js | 最能壓容量、最符合專案精神，每一塊都可控。以里程碑制控管自製引擎的時間風險（§10） |
| D-004 | 開發分工 | **Claude Code 主力實作，Lin 驗收**。每個里程碑交付可實玩的部署 URL，Lin 做決策、驗收與品味把關 | 計畫書以 AC 驗收制編排（§9） |

### 2.2 預設值（採 GPT 規格建議，Lin 可隨時推翻）

| 項目 | 預設 |
|---|---|
| 生成時機 | 每次載入時生成（方案 A）。瀏覽器 HTTP cache 已解決重複傳輸，不另做 IndexedDB 快取，除非生成時間超過 15 秒 |
| 關卡 | 固定種子、固定配置、單一關卡、主線線性加少量支線房 |
| 存檔與檢查點 | 第一版死亡從關卡起點重來。若 M2 實測流程過長再加中段檢查點 |
| 首領戰 | 提供 |
| 難度 | 單一標準難度 |
| 輸入 | 鍵盤滑鼠必備，M5 前提供按鍵重設 |
| 發行 | 免費，Netlify 正式 URL，可選 itch.io 頁面擴散 |

### 2.3 開放決策（暫緩，屆時再裁）

正式名稱（M5 前定案）。itch.io 上架與否。96KB 版的內容取捨。WebGPU 遷移（v1 一律 WebGL2）。原生 exe 挑戰版。

---

## 3 遊戲設計規格

### 3.1 核心循環

探索環境 → 發現敵人 → 戰鬥 → 取得彈藥或新武器 → 解鎖新區域 → 面對更強敵人 → 首領戰 → 結束畫面。

節奏原則：安靜探索與高強度戰鬥交替，每次拿到新武器後緊接一場能發揮它的戰鬥。

### 3.2 玩家能力與數值

能力：移動、跳躍、射擊、切換武器、撿取、互動（門與控制台）、暫停。不含蹲下、滑行、攀爬、翻滾、技能樹。

| 項目 | 初始值（可調） |
|---|---|
| 最大生命值 | 100 |
| 移動速度 | 6.0 m/s |
| 跳躍高度 | 1.2 m |
| 眼高與膠囊半徑 | 1.7 m 與 0.4 m |
| 受擊無敵時間 | 0.15 秒 |
| 死亡重啟時間 | 3 秒 |
| FOV | 預設 90，可調 70 至 110 |

### 3.3 武器規格

| 武器 | 型式 | 單發傷害 | 射速 | 彈藥上限 | 取得點 | 定位 |
|---|---|---|---|---|---|---|
| 脈衝手槍 | hitscan | 12 | 3 發/秒 | 120（起始滿） | 區域 A | 精準低傷，彈藥充足 |
| 散射槍 | hitscan，6 珠散佈 | 每珠 8 | 1.2 發/秒 | 48 | 區域 C | 近距高傷，走廊利器，對守衛體效率低 |
| 電漿步槍 | 投射物 20 m/s | 14 | 6 發/秒 | 180（彈藥稀少） | 區域 E | 中距壓制 |
| 能量砲 | 充能 1.2 秒 | 80 加範圍傷害 | 充能制 | 12 | 區域 F 前廳 | 首領戰資源（M3 交付，若時程吃緊可降級為選配） |

### 3.4 敵人規格

| 敵人 | HP | 速度 | 攻擊 | 行為要點 | 首次出現 |
|---|---|---|---|---|---|
| 巡行體 | 30 | 4.5 m/s | 近戰 10，間隔 0.8 秒 | 直線追擊，成群出現 | 區域 B |
| 射擊體 | 45 | 3.0 m/s | 投射 8，每 1.5 秒 | 尋找射線位置，保持中距，迫使玩家移動 | 區域 D |
| 守衛體 | 160 | 1.8 m/s | 近戰 25 加衝撞 | 正面減傷 50%，鼓勵繞背 | 區域 E |
| 首領：核心守護者 | 900 | 定點加平台移動 | 三模式 | 彈幕掃射、召喚巡行體、全場脈衝震波（可跳過）。場地含掩體 | 區域 F |

AI 一律有限狀態機：Idle → Detect → Chase 或 Reposition → Attack → Hurt → Dead。不用行為樹，不用機器學習。

### 3.5 難度與體驗目標

單一難度。新手數次嘗試內可通關，FPS 熟手首次即可通關。壓力來自敵人數量與空間配置，不以高精度射擊為門檻。首次通關 15 至 30 分鐘，熟練後 10 至 15 分鐘。

---

## 4 關卡設計規格

### 4.1 主題

一座遭生物機械體占領的地下能源設施。視覺元素：金屬牆面、管線、巨型機械、哥德拱門、深井、能源核心、橘紅警示燈、青藍能量光源。

### 4.2 區域規劃

| 區域 | 名稱 | 功能 | 敵人 | 取得物 |
|---|---|---|---|---|
| A | 甦醒室 | 出生點，學習移動視角 | 無 | 脈衝手槍 |
| B | 維修走廊 | 首次遭遇，教射擊與撿取 | 巡行體少量 | 彈藥 |
| C | 熔爐大廳 | 第一場大戰，垂直空間 | 巡行體群 | 散射槍、醫療 |
| D | 控制區 | 控制台開門機關 | 射擊體 | 彈藥、醫療 |
| E | 核心通道 | 高密度戰鬥，最終戰前壓迫感 | 守衛體加混編 | 電漿步槍 |
| F | 能源核心 | 首領戰與結束畫面 | 核心守護者 | 能量砲（前廳） |

### 4.3 限制

單一主關卡。主線線性，允許少量支線房（獎勵彈藥或醫療）。模組化牆面與結構重複使用，以參數變化（材質種子、色調、破損度）避免視覺單調。不做大型迷宮。

### 4.4 敘事（極簡）

開場三至五行文字：戰鬥人員在失控設施甦醒，機械系統正在融合生物組織，摧毀能源核心。場景內以警告標誌、控制台短訊息、燈光與音效傳達背景。無對話、無過場動畫、無語音。玩家不需理解故事也能通關。

---

## 5 美術規格

### 5.1 色彩規範

| 用途 | 建議值 | 說明 |
|---|---|---|
| 底黑 | #08090B | 場景最深處 |
| 金屬深灰 | #16191D | 牆面主體 |
| 鏽褐 | #4A3222 | 侵蝕與汙漬 |
| 能源青 | #35E0FF | 正常能源、電漿 |
| 警示橘 | #FF5A26 | 警告、敵對、傷害 |
| 治療綠 | #3DFF8E | 生命與恢復物品 |
| 互動白 | #F2F2F2 | 互動提示、UI 主文字 |

原則：低彩度背景加高彩度能量光源，強烈明暗對比。色彩提示不只依賴顏色（形狀與圖示並行，無障礙要求）。

### 5.2 建模原則

模型由基礎幾何組合生成：方塊、圓柱、球、錐、平面、擠壓輪廓、旋轉曲面（lathe）。每個模型保存的是**生成參數與步驟**，不是頂點資料傾印。硬編一大串頂點陣列視同違反零素材檔精神。

### 5.3 材質原則

程序化材質管線的節點操作：value 與 perlin noise、fbm、voronoi、gradient、color ramp、blur、edge detect、distortion、height 轉 normal、roughness 與 metallic 遮罩。預設解析度 512 × 512，品質設定可降 256 × 256。

### 5.4 特效清單

槍口火光、能量彈、爆炸、火花、蒸氣、敵人死亡溶解、螢幕受傷暈影。全部以 shader 與簡單粒子實作。

---

## 6 技術規格

### 6.1 平台目標

| 項目 | 規格 |
|---|---|
| 執行環境 | 桌面瀏覽器：Chrome、Edge、Firefox、Safari 各最近兩個大版 |
| 圖形 API | WebGL2（不依賴 WebGPU） |
| 音訊 | Web Audio API 全合成 |
| 輸入 | Pointer Lock API 加鍵盤。觸控裝置顯示不支援說明頁 |
| 儲存 | localStorage 僅存設定（靈敏度、音量、FOV、品質、按鍵），不存進度 |
| 後端 | 無。純靜態站，Netlify 部署 |

### 6.2 技術選型

| 項目 | 選擇 | 強制規則 |
|---|---|---|
| 語言 | TypeScript（strict） | 型別只在編譯期，不影響容量 |
| 開發建置 | esbuild dev server | 快速迭代 |
| 產線建置 | rollup 加 terser，單一 bundle，inline 進單一 index.html | 單檔精神；M6 階段再加 Roadroller 等極限壓縮 |
| 執行期依賴 | **零 runtime dependency**：package.json 的 dependencies 必須為空 | CI 檢查（§7.5）。渲染、數學、RNG、音訊全自寫 |
| 測試 | Playwright（真瀏覽器閘門）加 node:test（純邏輯單元） | Node 綠不等於瀏覽器能跑，見 §8.1 |

### 6.3 模組架構

| 模組 | 責任 |
|---|---|
| core | 主迴圈、時間步進、resize、pointer lock、輸入、可見性暫停 |
| gfx | WebGL2 封裝（shader 編譯、FBO、mesh buffer）、前向渲染器、粒子 |
| rng | 決定性亂數：mulberry32 加字串 hash 命名空間 |
| procgen/texture | GPU render-to-texture 材質生成管線 |
| procgen/mesh | 基礎幾何、extrude、lathe、合併、法線 |
| procgen/level | 房間圖、模組擺放、碰撞資料、敵人與物品配置（**純 CPU**） |
| audio | 合成引擎（oscillator、noise、envelope、biquad、waveshaper、feedback delay 殘響）、SFX preset、音樂步進序列器 |
| game | 玩家控制器、武器、戰鬥、敵人 FSM、撿取、門與觸發、關卡流程 |
| ui | HUD（血量、武器、彈藥、準星、互動提示）、主選單、暫停、設定、結束畫面 |

### 6.4 程序化生成規格（本案最重要技術段）

**種子策略**：全域主種子 96000。每個資產的種子由 `assetSeed = hash(masterSeed, assetName)` 導出（fnv1a 之類的字串 hash），例如 `texture.metal_wall`、`enemy.crawler`、`level.main`。不必手工維護種子清單，需要覆寫時才在 override 表指定。

**決定性鐵則（架構級規則）**：

- 影響玩法的生成（關卡佈局、碰撞、敵人與物品位置、數值）一律 CPU、一律定點或明確 IEEE 運算順序，要求**跨機器逐位元一致**。驗證方式：載入完成後輸出 `levelHash`（關卡資料的內容 hash），與 repo 內的 golden 值比對。
- 只影響外觀的生成（材質、粒子外觀）允許 GPU 產生，接受不同廠商間的微小差異，驗收標準是「無嚴重視覺差異」而非逐位元一致。

**生成流程**（載入時，附進度條）：初始化 GL 與 Audio → CPU 生成關卡資料與碰撞 → GPU 生成材質組 → CPU 生成模型網格 → 建立 GPU buffer → 合成音效 preset 預熱 → 進入主選單。生成過程**分幀執行**（每幀限時 8ms 左右），避免主執行緒凍結與瀏覽器無回應警告。

**失敗處理**：任何生成階段失敗都要浮上可見錯誤畫面（含階段名稱），禁止靜默跳過。每階段記錄產出計數，健康判定以「通過驗證的存活數」為準，不以「有產出」為準。

### 6.5 音訊合成

全部音效以 Web Audio 節點圖即時合成：武器射擊（每把音色可辨識）、命中、敵人受傷與死亡、玩家受傷、撿取、門與機械、警報、爆炸、UI。音樂為步進序列器驅動合成器：工業電子加 dark ambient，低頻 drone、金屬敲擊節奏、合成器脈衝，無人聲。探索與戰鬥兩種狀態，交叉淡入淡出。

瀏覽器限制：AudioContext 必須在使用者手勢後啟動，「點擊開始」畫面同時作為手勢入口。

### 6.6 物理與碰撞

玩家 capsule 對關卡靜態 AABB 格。hitscan 用 raycast，投射物用球體掃掠。不做剛體物理模擬。

### 6.7 網頁平台注意事項（設計時就要吃進去的約束）

| 約束 | 對策 |
|---|---|
| 音訊需使用者手勢 | 開始畫面點擊後才 resume AudioContext |
| Pointer lock 需手勢，Esc 會退出 | Esc 退出即進暫停選單，點擊繼續重新鎖定 |
| 分頁切換 rAF 停止 | visibilitychange 即暫停遊戲，dt 上限 50ms |
| WebGL context lost | 監聽並重新生成。程序化架構天然適合：所有資源可從種子重建 |
| Safari 浮點 FBO 支援差異 | 檢查 EXT_color_buffer_float，無則退 8-bit 材質管線 |
| 內顯效能 | 品質設定以「渲染解析度縮放」為主要手段 |
| 無音效裝置 | 不當機，靜音續玩 |

---

## 7 容量與效能預算

### 7.1 量測定義

**傳輸容量 = 產線 bundle 經 brotli -q 11 壓縮後的位元組總和**（等同 Netlify 實際傳輸）。工具 `npm run size` 輸出總量與模組分解（esbuild metafile）。所有容量目標皆指此值。

### 7.2 階梯目標

| 階段 | 傳輸容量上限 | 性質 |
|---|---|---|
| M1 概念驗證 | 5 MB（實務上應遠低於此） | 天花板，不為容量犧牲迭代速度 |
| M3 完整內容 | 2 MB | 開始有感控管 |
| M4 優化完成 | **1 MB** | 發行版承諾 |
| M6a 挑戰版 | 256 KB | 同內容極限壓縮 |
| M6b 挑戰版 | 96 KB | 內容取捨另議，向 .kkrieger 致敬 |

### 7.3 1MB 版建議分配（brotli 後）

| 模組 | 上限 |
|---|---|
| 核心程式與遊戲邏輯 | 500 KB |
| Shader 原始碼 | 150 KB |
| 生成參數與關卡資料 | 150 KB |
| 音訊 preset 與音樂 pattern | 50 KB |
| HTML、CSS、UI | 50 KB |
| 安全餘量 | 100 KB |

### 7.4 效能目標

| 項目 | 目標 |
|---|---|
| 幀率 | 1080p 平均 60 FPS（2020 年級中階獨顯與 Apple M1），1% low ≥ 45 |
| 內顯 | Iris Xe 級以品質設定（降渲染解析度）達可玩 |
| 載入生成時間 | 目標 5 秒內（M 系列），上限 15 秒，全程進度顯示 |
| 卡頓 | 遊戲中無超過 100 ms 的停頓 |
| 記憶體 | 峰值低於 512 MB |

### 7.5 自動守門（CI 與 pre-deploy，全部一鍵指令）

| 檢查 | 指令 | 判準 |
|---|---|---|
| 零素材檔 | `git ls-files` 過濾 `\.(png|jpe?g|gif|webp|svg|wav|mp3|ogg|flac|glb|gltf|fbx|obj|ttf|otf|woff2?)$` | 無輸出即 PASS。favicon 也以程式生成 data URI |
| 零 runtime deps | 讀 package.json 的 dependencies | 空物件即 PASS |
| 容量閘門 | `npm run size:check` 讀 budget.json 比對 brotli 總量 | 超過當前里程碑上限即 build fail |
| 決定性 | Playwright 讀 `levelHash` 比對 golden 值 | 相等即 PASS |

---

## 8 品質與驗證計畫

### 8.1 驗證分層原則（本機既有教訓的直接應用）

- **真瀏覽器閘門**：Node 環境測試只能驗純邏輯（數學、RNG、關卡生成演算法），驗不到 WebGL、Web Audio、pointer lock、worker。每個里程碑必過 Playwright 真瀏覽器驗收：頁面載入、無 console error、frame 計數前進、levelHash 正確、截圖存檔。
- **檢查與真相同層**：程序化內容的真相在像素與聲音，不在「程式跑完沒報錯」。每個里程碑保存關鍵畫面截圖為基準（golden screenshot），改動生成器後人工對照，防止「全綠但畫面壞了」。
- **有值不等於有效**：生成管線的健康判定放在驗證之後，計數用存活數。空模型、全黑材質、無聲音效都要能被驗收抓到（例如材質平均亮度與變異數的合理區間檢查、音效 buffer 非全零檢查）。

### 8.2 測試矩陣

| 面向 | 內容 |
|---|---|
| 功能 | 啟動、選單、移動、射擊、切槍、撿取、門與控制台、敵人 AI、傷害死亡重來、通關、設定各項 |
| 生成 | 同種子同結果（CPU 逐位元、GPU 視覺容差）、生成時間、記憶體峰值、中斷與失敗的可見性 |
| 相容 | Chrome、Edge、Firefox、Safari；macOS 與 Windows；獨顯與內顯；不同螢幕比例；無音效裝置 |
| 效能 | 平均 FPS 與 1% low（遊戲內建 F3 顯示）、生成各階段耗時、傳輸容量 |

### 8.3 里程碑驗收流程

Claude 自驗（自動守門全綠加實測）→ 部署 Netlify preview URL → **Lin 實玩驗收**（對照該里程碑 AC 逐條勾）→ 通過才進下一階段。實作與驗證隔離：關鍵交付另派 fresh-context 審查。

### 8.4 外部測試

M2 起至少 1 名非開發者無解說通關。M3 起 3 至 5 名，收斂「卡在哪、看不懂什麼、哪裡不好玩」三類回饋。

---

## 9 製作計畫（里程碑與 AC）

工作量以 Claude Code 主力實作的工作天粗估，日曆時間取決於 Lin 的驗收節奏。每個里程碑結束都有一個可實玩的部署 URL。

### M0 技術骨架（1 至 2 工作天）

交付：repo 鷹架、build 與 dev server、Netlify 部署管線、WebGL2 測試房、FPS 控制器、seeded RNG、容量報告工具、Playwright smoke。

- [ ] 開部署 URL，點擊後進入 pointer lock，WASD 移動、滑鼠視角、空白鍵跳躍
- [ ] 房間牆面貼一張程序化材質（noise 加 ramp 即可）
- [ ] `npm run size` 輸出 brotli 總量與模組分解
- [ ] 四項自動守門（§7.5）全綠
- [ ] Playwright smoke：載入、無 console error、frame 前進

### M1 概念驗證（3 至 5 工作天）

交付：脈衝手槍（hitscan、槍口特效、合成槍聲）、巡行體（程序化模型、完整 FSM）、傷害與死亡、重來、最小 HUD。

- [ ] 一個房間內擊殺 3 隻巡行體；玩家可被打死並在 3 秒後重來
- [ ] 槍聲、命中聲、死亡聲皆由合成器生成，repo 零音訊檔
- [ ] 敵人模型由參數生成，非頂點陣列傾印
- [ ] levelHash 兩次載入一致，Mac mini 與 MBP 跨機一致
- [ ] M 系列 60 FPS

### M2 垂直切片（2 至 3 週）

交付：區域 A 至 C、散射槍、撿取系統、門與觸發、主選單與暫停、色彩規範全面落地、第一軌 dark ambient、基本設定（靈敏度、音量、FOV）。

- [ ] 5 至 10 分鐘可完整遊玩
- [ ] Lin 以外至少 1 名測試者無解說通關
- [ ] 生成時間 5 秒內（M 系列），遊玩中無超過 100 ms 卡頓
- [ ] 四大瀏覽器 smoke 通過
- [ ] **玩法檢核點**：Lin 判定「這個方向好玩」才續建 M3，否則先調手感

### M3 完整內容（3 至 4 週）

交付：區域 D 至 F、電漿步槍、能量砲、射擊體、守衛體、首領戰、結束畫面、完整音效清單、雙態音樂系統、無障礙選項（關螢幕晃動、按鍵重設、色彩加形狀提示）。

- [ ] 首次通關 15 至 30 分鐘，含三種以上戰鬥情境
- [ ] 首領三攻擊模式可辨識、可學習、可通關
- [ ] 外部測試 3 人以上無解說通關
- [ ] 傳輸容量 ≤ 2 MB

### M4 容量、效能與相容（1 至 2 週）

- [ ] 傳輸容量 ≤ 1 MB（size gate 綠）
- [ ] 效能目標全數達標（§7.4），含內顯品質設定實測
- [ ] Windows 的 Chrome 與 Edge 實測（Lin 的 Windows 機）
- [ ] context lost 恢復、無音效裝置不當機、視窗縮放正常

### M5 發行候選（約 1 週）

- [ ] 無阻斷性 bug，§1.5 成功標準逐條核對全過
- [ ] 正式名稱定案、LICENSE 與 credits 完成、IP 檢查（§11）通過
- [ ] 正式 URL 發布；itch.io 頁面視裁決

### M6 壓縮挑戰版（選配，另行立項）

256KB 版：同內容極限壓縮（進階 minify、shader 壓縮、Roadroller 級打包、程式手工瘦身）。96KB 版：以 256KB 結果評估內容取捨，作為獨立的「96K edition」，不犧牲主發行版。

---

## 10 風險登錄表

| 風險 | 影響 | 機率 | 因應 |
|---|---|---|---|
| 程序化內容品質不足，畫面像技術 demo | 高 | 高 | M0 起建生成器 debug 頁（各材質與模型可視化預覽）；M2 設玩法與美術檢核點 |
| 追求容量導致不好玩 | 高 | 高 | D-002 階梯式已裁決；容量只在 M4 與 M6 集中處理 |
| 自製引擎時間失控 | 高 | 高 | 里程碑制，M2 檢核點重新評估範圍；引擎只做本遊戲需要的最小集合 |
| 生成時間過長 | 中 | 高 | 預算 5 秒、分幀生成、進度條；超過 15 秒才考慮快取方案 |
| GPU 廠商差異造成畫面不一致 | 高 | 中 | 玩法生成純 CPU 決定性；GPU 僅外觀，容差驗收 |
| Safari 相容性坑 | 中 | 高 | M0 起納入測試矩陣，不留到最後 |
| 音訊合成品質不佳 | 中 | 中 | 武器與環境音優先打磨；參考 sfxr 參數空間系統性調音 |
| 好玩度不足（單人專案盲點） | 高 | 中 | 每里程碑 Lin 實玩；M2 起外部測試者 |
| 素材檔偷渡破壞專案精神 | 中 | 中 | CI 零素材檔守門，逢 PR 必跑 |
| 專案變成純技術展示 | 高 | 中 | 每階段驗收都含「可玩性」條目，非只技術條目 |

---

## 11 智慧財產與授權

- 不使用 .kkrieger 名稱與 .theprodukkt 相關識別。工作代號 PROJECT 96 僅內部使用，正式名稱 M5 前另定並查重。
- 不複製原作關卡、敵人與武器造型、材質、音效、音樂；不使用來源不明的程式碼。
- 可借鑑：極小容量概念、程序化生成方法、demoscene 精神、黑暗科幻類型、短篇 FPS 結構。
- 零 runtime dependency 使發行版授權面乾淨；devDependencies 限 MIT 與同級授權。
- 字型不內嵌：UI 用 system-ui 字族，或程序化點陣字（M6 挑戰版用後者）。
- M5 產出 LICENSE 與 credits。

---

## 12 資料夾結構（建議）

```
fps-lin/
├── PLAN.md                  ← 本檔
├── docs/                    ← 日後需要拆分時（設計細節、決策紀錄）
├── src/
│   ├── core/  gfx/  rng/
│   ├── procgen/             ← texture/ mesh/ level/
│   ├── audio/  game/  ui/
│   └── shaders/
├── tools/                   ← 生成器 debug 頁、size-report
├── tests/                   ← playwright/、unit/、golden/（levelHash 與基準截圖）
├── scripts/                 ← build、size:check、check:assets、check:deps、deploy
└── dist/                    ← 產線輸出（gitignore）
```

---

## 13 本機開發環境注意（Lin 專屬，取自既往教訓）

- Dev server 埠用 **8330**：本機 3000、8000、5432 是 NursingFlow production 常駐埠；kill 任何 listener 前先 `ps -o command=` 驗證行程身分。
- 外接 SSD 專案：dev server 用 Bash 起（背景執行加 curl 健康檢查），不用 preview MCP（entitlement 會擋）。
- 跨機預覽（Mac mini 起、MBP 看）：server 綁 0.0.0.0，網址用 Tailscale IP。
- package.json 寫死 `packageManager` 與 `engines`，避免工具鏈版本漂移；鎖 lockfile。
- Netlify 部署用本機已登入的 CLI（`netlify deploy --dir dist`），bundle 極小無 payload 上限問題。

---

## 14 下一步

1. Lin 核可本計畫書（或指出要改的段落）。
2. 專案 git init 加 GitHub private repo（等 Lin 指示再動）。
3. 開 M0 派工：技術骨架與第一個可走動的部署 URL。
